name: ecellapp
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  bloc: ^8.1.0
  cupertino_icons: ^1.0.5
  data_connection_checker_nulls: ^0.0.2
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  flutter_launcher_icons: ^0.10.0
  get_it: ^7.2.0
  google_fonts: ^3.0.1
  http: ^0.13.5
#  legacy_buttons: ^0.0.1
#  internet_connection_checker: ^1.0.0+1
  logger: ^1.1.0
  pinput: ^2.2.16
  provider: ^6.0.3
  rxdart: ^0.27.5
  shared_preferences: ^2.0.15
  timer_count_down: ^2.2.1
  url_launcher: ^6.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  ios: true
  android: true
  image_path_ios: "assets/app-icon/app-icon.png"
  image_path_android: "assets/app-icon/app-icon.png"

flutter:
  assets:
    - assets/
    - assets/icons/
    - assets/basic/
    - assets/colorful/

  uses-material-design: true
